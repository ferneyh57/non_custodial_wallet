class CoinEntity {
  final String symbol;
  final String name;
  final double currentPrice;

  const CoinEntity({
    required this.symbol,
    required this.name,
    required this.currentPrice,
  });
}
